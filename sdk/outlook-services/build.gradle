apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile(group: 'com.microsoft.services.orc', name: 'orc-engine-android', version: '0.15.0')
}

apply from :'../java-libraries.gradle'
